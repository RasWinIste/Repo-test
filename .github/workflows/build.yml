name: SonarQube
on:
  push:
    branches:
      - main
  pull_request:
    types: [opened, synchronize]
jobs:
  build:
    name: Build and analyze
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0  # Shallow clones should be disabled for a better relevancy of analysis
      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: 17
          distribution: 'zulu' # Alternative distribution options are available.
      - name: Cache SonarQube packages
        uses: actions/cache@v4
        with:
          path: ~/.sonar/cache
          key: ${{ runner.os }}-sonar
          restore-keys: ${{ runner.os }}-sonar
      - name: Cache Maven packages
        uses: actions/cache@v4
        with:
          path: ~/.m2
          key: ${{ runner.os }}-m2-${{ hashFiles('**/pom.xml') }}
          restore-keys: ${{ runner.os }}-m2
      - name: Build and analyze
        env:
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
        run: mvn -B verify org.sonarsource.scanner.maven:sonar-maven-plugin:sonar -Dsonar.projectKey=RasWinIste_Repo-test


name: CodeQL PR-Only Analysis

on:
  pull_request:
    types: [opened, synchronize]

jobs:
  codeql:
    runs-on: ubuntu-latest
    permissions:
      security-events: write
      pull-requests: read
      contents: read

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Initialize CodeQL
      uses: github/codeql-action/init@v3
      with:
        languages: java
        queries: ./codeql

    - name: Build project
      run: mvn clean install -DskipTests

    - name: Perform CodeQL analysis
      uses: github/codeql-action/analyze@v3
      with:
        output: ./results
        format: sarif

#    - name: Get PR diff
#      run: |
#        git fetch origin ${{ github.event.pull_request.base.ref }}
#        git diff origin/${{ github.event.pull_request.base.ref }} --unified=0 > pr.diff

#    - name: Filter SARIF to changed lines
#      run: |
#        pip install sarif-tools
#        sarif filter --input ./results/codeql.sarif --diff pr.diff --output ./results/filtered.sarif

#    - name: Upload filtered SARIF
#      uses: actions/upload-artifact@v4
#      with:
#        name: filtered-codeql-results
#        path: ./results/filtered.sarif